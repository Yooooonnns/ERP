<Window x:Class="DigitalisationERP.Desktop.Views.TaskReportDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Tâche" Height="460" Width="620"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock x:Name="TitleText" Text="Tâche" FontSize="20" FontWeight="Bold"/>
            <TextBlock x:Name="MetaText" Margin="0,6,0,12" Foreground="Gray" TextWrapping="Wrap"/>
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="14"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <ComboBox x:Name="StatusCombo" materialDesign:HintAssist.Hint="Statut">
                <ComboBoxItem Content="ToDo" IsSelected="True"/>
                <ComboBoxItem Content="InProgress"/>
                <ComboBoxItem Content="Done"/>
                <ComboBoxItem Content="Blocked"/>
            </ComboBox>

            <TextBox Grid.Column="2" x:Name="ShortNoteTextBox" materialDesign:HintAssist.Hint="Note courte (optionnel)"/>
        </Grid>

        <TextBox Grid.Row="2" x:Name="ReportTextBox"
                 materialDesign:HintAssist.Hint="Rapport / feedback à remonter (optionnel)"
                 AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Annuler" Style="{StaticResource MaterialDesignFlatButton}" Margin="0,0,10,0" Click="Cancel_Click"/>
            <Button Content="Enregistrer" Style="{StaticResource MaterialDesignRaisedButton}" Click="Save_Click"/>
        </StackPanel>
    </Grid>
</Window>
