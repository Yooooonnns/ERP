<Page x:Class="DigitalisationERP.Desktop.Views.UsersManagementPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      Title="Gestion des Utilisateurs" Height="700" Width="1200">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Gestion des Utilisateurs" 
                      FontSize="28" 
                      FontWeight="Bold"
                      Margin="0,0,0,10"/>
            <TextBlock Text="Créez, modifiez et gérez les comptes utilisateurs" 
                      FontSize="14" 
                      Foreground="Gray"/>
        </StackPanel>

        <!-- Search & Actions -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="SearchBox"
                    Width="400"
                    HorizontalAlignment="Left"
                    materialDesign:HintAssist.Hint="Rechercher un utilisateur..."
                    Style="{StaticResource MaterialDesignOutlinedTextBox}">
                <TextBox.InputBindings>
                    <KeyBinding Key="Enter" Command="{Binding SearchCommand}"/>
                </TextBox.InputBindings>
            </TextBox>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="Créer Utilisateur" 
                       Style="{StaticResource MaterialDesignRaisedButton}"
                       Background="#2196F3"
                       Height="40"
                      Margin="0,0,10,0"
                      Click="CreateUser_Click">
                    <Button.ContentTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="AccountPlus" 
                                                        Width="20" 
                                                        Height="20"
                                                        VerticalAlignment="Center"/>
                                <TextBlock Text="{Binding}" 
                                          Margin="8,0,0,0"
                                          VerticalAlignment="Center"/>
                            </StackPanel>
                        </DataTemplate>
                    </Button.ContentTemplate>
                </Button>

                <Button Content="Exporter" 
                       Style="{StaticResource MaterialDesignOutlinedButton}"
                       Height="40"
                      Margin="0,0,10,0"
                      Click="Export_Click"/>

                <ComboBox Width="120" 
                         materialDesign:HintAssist.Hint="Filtrer rôle">
                    <ComboBoxItem Content="Tous"/>
                    <ComboBoxItem Content="S_USER"/>
                    <ComboBoxItem Content="SAP_ALL"/>
                    <!-- Production Department -->
                    <ComboBoxItem Content="Z_PROD_MANAGER"/>
                    <ComboBoxItem Content="Z_PROD_PLANNER"/>
                    <ComboBoxItem Content="Z_PROD_OPERATOR"/>
                    <!-- Maintenance Department -->
                    <ComboBoxItem Content="Z_MAINT_MANAGER"/>
                    <ComboBoxItem Content="Z_MAINT_PLANNER"/>
                    <ComboBoxItem Content="Z_MAINT_TECH"/>
                    <!-- Warehouse Department -->
                    <ComboBoxItem Content="Z_WM_MANAGER"/>
                    <ComboBoxItem Content="Z_WM_CLERK"/>
                    <ComboBoxItem Content="Z_MM_BUYER"/>
                    <!-- Quality Department -->
                    <ComboBoxItem Content="Z_QM_MANAGER"/>
                    <ComboBoxItem Content="Z_QM_INSPECTOR"/>
                    <!-- Robotics & IoT -->
                    <ComboBoxItem Content="Z_ROBOT_ADMIN"/>
                    <ComboBoxItem Content="Z_ROBOT_OPERATOR"/>
                    <ComboBoxItem Content="Z_IOT_ADMIN"/>
                    <ComboBoxItem Content="Z_IOT_MONITOR"/>
                </ComboBox>
            </StackPanel>
        </Grid>

        <!-- Users DataGrid -->
        <materialDesign:Card Grid.Row="2">
            <DataGrid x:Name="UsersDataGrid"
                     AutoGenerateColumns="False"
                     CanUserAddRows="False"
                     CanUserDeleteRows="False"
                     SelectionMode="Single"
                     HeadersVisibility="Column"
                     RowHeight="60">
                <DataGrid.Columns>
                    <!-- Avatar -->
                    <DataGridTemplateColumn Header="" Width="60">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Width="40" 
                                       Height="40" 
                                       CornerRadius="20"
                                       Background="{Binding AvatarColor}">
                                    <TextBlock Text="{Binding Initials}" 
                                             FontSize="16" 
                                             FontWeight="Bold"
                                             Foreground="White"
                                             HorizontalAlignment="Center"
                                             VerticalAlignment="Center"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- Name & Email -->
                    <DataGridTemplateColumn Header="Utilisateur" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel>
                                    <TextBlock Text="{Binding FullName}" 
                                             FontSize="14" 
                                             FontWeight="SemiBold"/>
                                    <TextBlock Text="{Binding Email}" 
                                             FontSize="12" 
                                             Foreground="Gray"
                                             Margin="0,2,0,0"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- Role -->
                    <DataGridTemplateColumn Header="Rôle" Width="150">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Background="{Binding RoleColor}" 
                                       CornerRadius="4" 
                                       Padding="10,4">
                                    <TextBlock Text="{Binding Role}" 
                                             FontSize="12" 
                                             Foreground="White"
                                             HorizontalAlignment="Center"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- Department -->
                    <DataGridTextColumn Header="Département" 
                                       Binding="{Binding Department}" 
                                       Width="140"/>

                    <!-- Last Login -->
                    <DataGridTemplateColumn Header="Dernière connexion" Width="160">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="ClockOutline" 
                                                           Width="14" 
                                                           Height="14"
                                                           VerticalAlignment="Center"
                                                           Foreground="Gray"/>
                                    <TextBlock Text="{Binding LastLogin}" 
                                             FontSize="12" 
                                             Foreground="Gray"
                                             Margin="5,0,0,0"
                                             VerticalAlignment="Center"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- Status -->
                    <DataGridTemplateColumn Header="Statut" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Ellipse Width="8" 
                                            Height="8" 
                                            Fill="{Binding StatusColor}"
                                            VerticalAlignment="Center"/>
                                    <TextBlock Text="{Binding Status}" 
                                             FontSize="12"
                                             Margin="6,0,0,0"
                                             VerticalAlignment="Center"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- Actions -->
                    <DataGridTemplateColumn Header="Actions" Width="150">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Style="{StaticResource MaterialDesignIconButton}"
                                           ToolTip="Modifier"
                                           Width="32"
                                           Height="32"
                                           Click="EditUser_Click">
                                        <materialDesign:PackIcon Kind="Pencil"/>
                                    </Button>
                                    <Button Style="{StaticResource MaterialDesignIconButton}"
                                           ToolTip="Désactiver"
                                           Width="32"
                                           Height="32"
                                           Click="ToggleUserStatus_Click">
                                        <materialDesign:PackIcon Kind="AccountOff"/>
                                    </Button>
                                    <Button Style="{StaticResource MaterialDesignIconButton}"
                                           ToolTip="Supprimer"
                                           Width="32"
                                           Height="32"
                                           Foreground="#F44336"
                                           Click="DeleteUser_Click">
                                        <materialDesign:PackIcon Kind="Delete"/>
                                    </Button>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </materialDesign:Card>
    </Grid>
</Page>
