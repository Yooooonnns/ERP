<Window x:Class="DigitalisationERP.Desktop.Views.LeaveRequestDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Demande de congé" Height="360" Width="520"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Demande de congé" FontSize="20" FontWeight="Bold"/>
        <TextBlock Grid.Row="1" Margin="0,6,0,18" Foreground="Gray"
                   Text="Choisissez une période et indiquez la raison."/>

        <Grid Grid.Row="2" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <DatePicker x:Name="StartDatePicker" materialDesign:HintAssist.Hint="Début"/>
            <DatePicker Grid.Column="2" x:Name="EndDatePicker" materialDesign:HintAssist.Hint="Fin"/>
        </Grid>

        <TextBox Grid.Row="3" x:Name="ReasonTextBox"
                 materialDesign:HintAssist.Hint="Raison"
                 AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Annuler" Style="{StaticResource MaterialDesignFlatButton}" Margin="0,0,10,0" Click="Cancel_Click"/>
            <Button Content="Envoyer" Style="{StaticResource MaterialDesignRaisedButton}" Click="Submit_Click"/>
        </StackPanel>
    </Grid>
</Window>
