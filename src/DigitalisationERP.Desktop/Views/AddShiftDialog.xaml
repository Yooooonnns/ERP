<Window x:Class="DigitalisationERP.Desktop.Views.AddShiftDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Ajouter un shift" Height="460" Width="560"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="Ajouter un shift" FontSize="20" FontWeight="Bold"/>
            <TextBlock Margin="0,6,0,16" Foreground="Gray" Text="Planification simple (stockage local)."/>
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="14"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="EmployeeTextBox" materialDesign:HintAssist.Hint="Employé (email/id)"/>

            <DatePicker Grid.Row="1" x:Name="DatePicker" materialDesign:HintAssist.Hint="Date"/>

            <ComboBox Grid.Row="1" Grid.Column="2" x:Name="SegmentCombo" materialDesign:HintAssist.Hint="Segment">
                <ComboBoxItem Content="Matin" IsSelected="True"/>
                <ComboBoxItem Content="Après-midi"/>
                <ComboBoxItem Content="Nuit"/>
            </ComboBox>

            <TextBox Grid.Row="2" x:Name="StartTextBox" materialDesign:HintAssist.Hint="Début (HH:mm)"/>
            <TextBox Grid.Row="2" Grid.Column="2" x:Name="EndTextBox" materialDesign:HintAssist.Hint="Fin (HH:mm)"/>

            <TextBox Grid.Row="3" x:Name="LocationTextBox" materialDesign:HintAssist.Hint="Poste / Lieu"/>
            <TextBox Grid.Row="3" Grid.Column="2" x:Name="NotesTextBox" materialDesign:HintAssist.Hint="Notes (optionnel)"/>

            <TextBlock Grid.Row="4" Grid.ColumnSpan="3" Foreground="Gray" TextWrapping="Wrap"
                       Text="Astuce: utilisez les IDs des comptes de test (ex: prod.operator@test.com)."/>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Annuler" Style="{StaticResource MaterialDesignFlatButton}" Margin="0,0,10,0" Click="Cancel_Click"/>
            <Button Content="Ajouter" Style="{StaticResource MaterialDesignRaisedButton}" Click="Add_Click"/>
        </StackPanel>
    </Grid>
</Window>
