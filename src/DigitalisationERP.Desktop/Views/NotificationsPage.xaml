<Page x:Class="DigitalisationERP.Desktop.Views.NotificationsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      Background="{DynamicResource MaterialDesignPaper}"
      Title="Notifications">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="20">
            <TextBlock Text="Notifications" FontSize="28" FontWeight="Bold"/>
            <Button x:Name="ClearAllButton" Style="{StaticResource MaterialDesignRaisedSecondary}" 
                    Margin="0,15,0,0" Click="ClearAll_Click" Width="120" HorizontalAlignment="Left">
                <TextBlock Text="Clear All" FontSize="13"/>
            </Button>
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="250"/>
            </Grid.ColumnDefinitions>

            <!-- Notifications List -->
            <materialDesign:Card Grid.Column="0" Margin="20,0,10,20" Padding="20">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Alert Center" FontSize="16" FontWeight="Bold" Margin="0,0,0,15"/>

                    <ListBox x:Name="NotificationsList" Grid.Row="1" ScrollViewer.VerticalScrollBarVisibility="Auto">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="10,12">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <materialDesign:PackIcon Grid.Column="0" Kind="{Binding Icon}" Width="24" Height="24" 
                                                               VerticalAlignment="Top" Margin="0,0,12,0"/>

                                        <StackPanel Grid.Column="1">
                                            <TextBlock Text="{Binding Title}" FontSize="13" FontWeight="Bold"/>
                                            <TextBlock Text="{Binding Message}" FontSize="12" Foreground="#666" TextWrapping="Wrap" Margin="0,5,0,0"/>
                                            <TextBlock Text="{Binding Timestamp}" FontSize="10" Foreground="#999" Margin="0,5,0,0"/>
                                        </StackPanel>

                                        <Button Grid.Column="2" Style="{StaticResource MaterialDesignIconButton}" 
                                               ToolTip="Dismiss" VerticalAlignment="Top">
                                            <materialDesign:PackIcon Kind="Close" Width="20" Height="20"/>
                                        </Button>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </materialDesign:Card>

            <!-- Sidebar - Stats -->
            <materialDesign:Card Grid.Column="1" Margin="0,20,20,20" Padding="15">
                <StackPanel>
                    <TextBlock Text="Notification Stats" FontSize="13" FontWeight="Bold" Margin="0,0,0,15"/>

                    <Border Background="#F44336" CornerRadius="5" Padding="12" Margin="0,0,0,10">
                        <StackPanel>
                            <TextBlock Text="Critical" FontSize="11" Foreground="White"/>
                            <TextBlock Text="2" FontSize="20" FontWeight="Bold" Foreground="White"/>
                        </StackPanel>
                    </Border>

                    <Border Background="#FF9800" CornerRadius="5" Padding="12" Margin="0,0,0,10">
                        <StackPanel>
                            <TextBlock Text="Warnings" FontSize="11" Foreground="White"/>
                            <TextBlock Text="5" FontSize="20" FontWeight="Bold" Foreground="White"/>
                        </StackPanel>
                    </Border>

                    <Border Background="#4CAF50" CornerRadius="5" Padding="12">
                        <StackPanel>
                            <TextBlock Text="Info" FontSize="11" Foreground="White"/>
                            <TextBlock Text="8" FontSize="20" FontWeight="Bold" Foreground="White"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </materialDesign:Card>
        </Grid>
    </Grid>
</Page>
